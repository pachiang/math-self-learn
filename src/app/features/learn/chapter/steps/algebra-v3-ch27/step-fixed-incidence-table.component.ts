import{Component,computed,signal}from'@angular/core';import{binaryStrings,fixedStrings,rotate}from'./burnside-model';
@Component({selector:'app-algebra-v3-fixed-incidence-table',standalone:true,template:`
<article class="algebra-v3-lesson alg-ch27-lesson"><header class="hero"><p class="eyebrow">Abstract Algebra · 27.2</p><h2>先不數 necklaces；改數哪些 symmetry–picture pairs 完全不動</h2><p class="lede">一格代表一個 incidence pair：把 row rotation 作用到 column picture。只有 output 仍是原 picture時打上 FIXED。</p></header><section class="prediction"><p class="kicker">先預測</p><h3>180° rotation 固定的 pictures 會比 90° rotation 多嗎？</h3><div class="choice-row"><button type="button" (click)="prediction.set(true)">會，對面兩顆只需同色</button><button type="button" (click)="prediction.set(false)">不會，non-identity 都固定一樣多</button></div>@if(prediction()!==null){<p class="feedback" [class.warning]="!prediction()">{{prediction()?'對。180° 有兩組 position cycles，各組可獨立選色，共 4 張；90° 把四格綁成一組，只剩 2 張。':'不同 symmetries 的 cycle structure 不同，fixed counts 不必相同。'}}</p>}</section>
<section class="lab"><div class="lab-heading"><div><p class="kicker">Fixed-incidence table</p><h3>逐 row 掃描 16 張 pictures</h3></div><p>實心 ✓ 是 fixed pair；虛線 · 是 moved pair。每列右端同步累積 row total。</p></div><div class="stage incidence-stage"><section class="incidence-table"><header><span>ROT / PICTURE</span>@for(x of all;track x){<small>{{x}}</small>}<b>FIXED</b></header>@for(k of steps;track k;let row=$index){<article [class.revealed]="rowVisible(row)"><strong>{{k*90}}°</strong>@for(x of all;track x){<i [class.fixed]="rowVisible(row)&&isFixed(x,k)">{{cellLabel(row,x,k)}}</i>}<b>{{rowTotalLabel(row,k)}}</b></article>}</section><section class="incidence-console"><p class="kicker">ROWS REVEALED {{revealed()}} / 4</p><div><span>ROW TOTALS</span><b>{{totalsLabel()}}</b><small>FIXED PAIRS COUNTED SO FAR</small></div><div><span>TOTAL INCIDENCES</span><b>{{total()}}</b><small>WHEN COMPLETE: 24</small></div><button type="button" class="primary" [disabled]="revealed()>=4" (click)="advance()">揭露下一個 rotation</button><button type="button" (click)="revealed.set(4)">揭露全部</button><button type="button" (click)="revealed.set(0)">重設</button></section></div></section>
<aside class="insight-card"><div class="insight-visual" aria-hidden="true"><span>sum by symmetry rows</span><i>=</i><span>16 + 2 + 4 + 2 = 24 fixed pairs</span></div><p><strong>Burnside 的原料不是 objects，而是 fixed incidences。</strong>我們先按 symmetry 分組數完；下一幕會把同一批 24 格改按 orbit 分組。</p></aside><section class="transfer"><p class="kicker">遷移</p><h3>identity row 為什麼固定全部 16 張？</h3><div class="choice-row"><button type="button" (click)="transfer.set(true)">identity 不改任何 picture</button><button type="button" (click)="transfer.set(false)">因為 identity 只固定 symmetric pictures</button></div>@if(transfer()!==null){<p class="feedback" [class.warning]="!transfer()">{{transfer()?'對。identity contribution 永遠是整個 object set。':'identity 對每個 object 都輸出自己，不需要 object 本身有額外 symmetry。'}}</p>}</section><section class="secondary"><p>SECONDARY LAYER</p><details><summary>目前 double count 的集合</summary><div>我們正在數所有滿足 g·x=x 的 ordered pairs (g,x)。按 g 分組，就得到各 row 的 |Fix(g)| 相加；下一頁按 x 所屬 orbit 重組同一集合。</div></details></section></article>`})
export class AlgebraV3FixedIncidenceTableComponent{readonly all=binaryStrings(4);readonly steps=[0,1,2,3];readonly revealed=signal(0);readonly prediction=signal<boolean|null>(null);readonly transfer=signal<boolean|null>(null);readonly total=computed(()=>this.steps.slice(0,this.revealed()).reduce((s,k)=>s+this.fixedCount(k),0));isFixed(x:string,k:number){return rotate(x,k)===x}fixedCount(k:number){return fixedStrings(4,k).length}rowVisible(row:number){return row<this.revealed()}cellLabel(row:number,x:string,k:number){return this.rowVisible(row)?(this.isFixed(x,k)?'✓':'·'):'?'}rowTotalLabel(row:number,k:number){return this.rowVisible(row)?String(this.fixedCount(k)):'—'}advance(){this.revealed.update(v=>Math.min(4,v+1))}totalsLabel(){return this.steps.map((k,i)=>i<this.revealed()?this.fixedCount(k):'—').join(' + ')}}
