import{Component,computed,signal}from'@angular/core';import{fixedStrings,gcd,positionCycles}from'./burnside-model';
@Component({selector:'app-algebra-v3-necklace-cycle-counter',standalone:true,template:`
<article class="algebra-v3-lesson alg-ch27-lesson"><header class="hero"><p class="eyebrow">Abstract Algebra · 27.4</p><h2>要數 rotation 固定的 necklaces，只要看它把 bead positions 綁成幾個 cycles</h2><p class="lede">fixed coloring 不能在同一 position cycle 裡換色。每個 cycle 可獨立選黑或白，所以 cycle 數直接控制 fixed count。</p></header><section class="prediction"><p class="kicker">先預測</p><h3>6 顆 beads 旋轉 2 格，positions 會被綁成幾個 cycles？</h3><div class="choice-row"><button type="button" (click)="prediction.set(2)">2 個 cycles</button><button type="button" (click)="prediction.set(3)">3 個 cycles</button></div>@if(prediction()){<p class="feedback" [class.warning]="prediction()!==2">{{prediction()===2?'對。0→2→4→0 與 1→3→5→1，正好 gcd(6,2)=2。':'逐步追蹤會得到偶數 positions 一圈、奇數 positions 一圈，共 2 個。'}}</p>}</section>
<section class="lab"><div class="lab-heading"><div><p class="kicker">Necklace cycle counter</p><h3>調整 n 與 rotation step k</h3></div><p>同 cycle beads 使用相同 pattern 與 C-label；顏色不是唯一線索。</p></div><div class="cycle-controls"><fieldset><legend>BEADS n</legend>@for(v of sizes;track v){<button type="button" [attr.aria-pressed]="n()===v" (click)="setN(v)">{{v}}</button>}</fieldset><fieldset><legend>ROTATE k STEPS</legend>@for(v of steps();track v){<button type="button" [attr.aria-pressed]="k()===v" (click)="k.set(v)">{{v}}</button>}</fieldset></div><div class="stage cycle-counter-stage"><section class="cycle-ring" [attr.aria-label]="ringLabel()">@for(i of positions();track i){<article [style.transform]="positionStyle(i)" [attr.data-cycle]="cycleIndex(i)%4"><strong>{{i}}</strong><small>C{{cycleIndex(i)+1}}</small></article>}<div><span>ROTATE</span><b>+{{k()}}</b></div></section><section class="cycle-list">@for(cycle of cycles();track cycle[0];let i=$index){<article [attr.data-cycle]="i%4"><span>C{{i+1}}</span><b>{{cycle.join(' → ')}} → {{cycle[0]}}</b><small>ONE SHARED COLOR CHOICE</small></article>}</section><section class="necklace-console"><p class="kicker">FIXED-COLORING COUNTER</p><div><span>POSITION CYCLES</span><b>gcd({{n()}}, {{k()}}) = {{cycles().length}}</b></div><div><span>FIXED BINARY COLORINGS</span><b>2<sup>{{cycles().length}}</sup> = {{fixedCount()}}</b></div><div class="average-table">@for(v of steps();track v){<span>k={{v}}: {{fixedFor(v)}}</span>}</div><div class="necklace-verdict">AVERAGE = {{orbitCount()}} NECKLACES</div></section></div></section>
<aside class="insight-card"><div class="insight-visual" aria-hidden="true"><span>count cycles of each symmetry</span><i>→ fixed objects</i><span>average → orbits</span></div><p><strong>Burnside 把「避免 symmetry duplicates」改成一串容易的 fixed-point problems。</strong>對 binary necklaces，每個 rotation cycle 只需做一次二選一。</p></aside><section class="transfer"><p class="kicker">遷移</p><h3>若每顆 bead 有 3 種顏色，c 個 position cycles 會有幾個 fixed colorings？</h3><div class="choice-row"><button type="button" (click)="transfer.set(true)">3ᶜ</button><button type="button" (click)="transfer.set(false)">2ᶜ</button></div>@if(transfer()!==null){<p class="feedback" [class.warning]="!transfer()">{{transfer()?'對。每個 cycle 獨立選 3 色，binary 的 2 只需換成 q。':'底數是每個 cycle 的顏色選擇數；3 colors 就是 3。'}}</p>}</section><section class="secondary"><p>SECONDARY LAYER</p><details><summary>一般 q-color necklace formula</summary><div>Cₙ 的 rotation k 在 n 個 positions 上有 gcd(n,k) 個 cycles，因此固定「q 的 gcd(n,k) 次方」個 colorings。Burnside 把 k=0 到 n−1 的 fixed counts 相加，再除以 n。</div></details></section></article>`})
export class AlgebraV3NecklaceCycleCounterComponent{readonly sizes=[3,4,5,6,7,8];readonly n=signal(6);readonly k=signal(2);readonly prediction=signal<number|null>(null);readonly transfer=signal<boolean|null>(null);readonly steps=computed(()=>Array.from({length:this.n()},(_,i)=>i));readonly positions=computed(()=>this.steps());readonly cycles=computed(()=>positionCycles(this.n(),this.k()));readonly fixedCount=computed(()=>2**gcd(this.n(),this.k()));readonly orbitCount=computed(()=>this.steps().reduce((s,k)=>s+this.fixedFor(k),0)/this.n());setN(v:number){this.n.set(v);this.k.set(Math.min(this.k(),v-1))}cycleIndex(p:number){return this.cycles().findIndex(c=>c.includes(p))}fixedFor(k:number){return fixedStrings(this.n(),k).length}positionStyle(i:number){return`rotate(${i*360/this.n()}deg) translateY(-122px) rotate(${-i*360/this.n()}deg)`}ringLabel(){return`${this.n()} positions rotated ${this.k()} steps form ${this.cycles().length} cycles`}}
