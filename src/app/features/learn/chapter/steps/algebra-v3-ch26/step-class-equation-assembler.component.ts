import{Component,computed,signal}from'@angular/core';import{D3_ELEMENTS,label}from'../algebra-v3-ch16/d3-model';import{conjugacyPartition}from'./class-model';
@Component({selector:'app-algebra-v3-class-equation-assembler',standalone:true,template:`
<article class="algebra-v3-lesson alg-ch26-lesson"><header class="hero"><p class="eyebrow">Abstract Algebra · 26.3</p><h2>Class equation 把整個群按 re-framing action type 分帳</h2><p class="lede">Conjugacy classes partition G：每個 element 恰好進一包。singleton packets 是任何 frame 都不改名的 center；其餘 packets 收集真正會互相 re-frame 的 families。</p></header><section class="prediction"><p class="kicker">先預測</p><h3>一個 conjugacy class 只有 h 自己，能推出 h 在 center 嗎？</h3><div class="choice-row"><button type="button" (click)="prediction.set(true)">能，所有 frames 都固定 h</button><button type="button" (click)="prediction.set(false)">不能，singleton 與 center 無關</button></div>@if(prediction()!==null){<p class="feedback" [class.warning]="!prediction()">{{prediction()?'對。class 沒有其他 outputs，代表每個 khk⁻¹ 都等於 h。':'singleton orbit 就是 global fixed point，因此 h 與所有 k commute。'}}</p>}</section>
<section class="lab"><div class="lab-heading"><div><p class="kicker">Class-equation assembler</p><h3>逐包裝入，直到六個 elements 恰好用完</h3></div><p>每包使用不同 stripe／border pattern；總和顯示的是 partition accounting，不只是一串數字。</p></div><div class="stage equation-stage"><section class="element-pool">@for(x of elements;track x){<article [class.assigned]="isAssigned(x)"><strong>{{name(x)}}</strong><small>{{isAssigned(x)?'ASSIGNED TO CLASS':'UNASSIGNED'}}</small></article>}</section><section class="class-assembly">@for(group of visible();track group[0];let i=$index){<article [class.singleton]="group.length===1"><header><span>CLASS {{i+1}}</span><strong>{{classLabel(group)}}</strong></header><div>@for(x of group;track x){<b>{{name(x)}}</b>}</div><footer>{{group.length===1?'CENTER SINGLETON':group.length+'-ELEMENT FAMILY'}}</footer></article>}@if(count()<classes.length){<article class="pending"><span>NEXT PACKET</span><strong>size {{classes[count()].length}}</strong></article>}</section><section class="equation-console"><p class="kicker">ASSEMBLED {{assignedCount()}} / 6</p><div class="sum-equation"><span>|D₃|</span><b>6 = {{sumLabel()}}</b><small>{{count()===classes.length?'ALL ELEMENTS ACCOUNTED FOR':'PARTITION INCOMPLETE'}}</small></div><button type="button" class="primary" [disabled]="count()>=classes.length" (click)="advance()">加入下一個 class</button><button type="button" (click)="count.set(classes.length)">全部組裝</button><button type="button" (click)="count.set(0)">重設</button></section></div></section>
<aside class="insight-card"><div class="insight-visual" aria-hidden="true"><span>center singletons</span><i>+</i><span>noncentral classes</span><i>=</i><span>whole G</span></div><p><strong>Class equation 是 conjugation-orbit partition 的總帳。</strong>它把 group order 拆成 center 的 singleton contributions 與所有 noncentral action-type packets。</p></aside><section class="transfer"><p class="kicker">遷移</p><h3>若 G abelian，class equation 會長什麼樣？</h3><div class="choice-row"><button type="button" (click)="transfer.set(true)">全部都是 size-1 classes</button><button type="button" (click)="transfer.set(false)">只有一個巨大 class</button></div>@if(transfer()!==null){<p class="feedback" [class.warning]="!transfer()">{{transfer()?'對。每個 element 都在 center，任何 conjugation 都不改名。':'abelian 時 conjugation action 是 trivial，每個 orbit 都只有自己的 element。'}}</p>}</section><section class="secondary"><p>SECONDARY LAYER</p><details><summary>Class equation 的正式版本</summary><div>選每個 noncentral conjugacy class 的 representative xᵢ。由 conjugacy partition 與 class-size formula，|G|=|Z(G)| 加上所有 indices [G:C_G(xᵢ)] 的總和。D₃ 的具體分帳是 6=1+2+3。</div></details></section></article>`})
export class AlgebraV3ClassEquationAssemblerComponent{readonly elements=D3_ELEMENTS;readonly classes=conjugacyPartition();readonly count=signal(0);readonly prediction=signal<boolean|null>(null);readonly transfer=signal<boolean|null>(null);readonly visible=computed(()=>this.classes.slice(0,this.count()));readonly assigned=computed(()=>this.visible().flat());readonly assignedCount=computed(()=>this.assigned().length);name=label;advance(){this.count.update(v=>Math.min(this.classes.length,v+1))}isAssigned(x:(typeof D3_ELEMENTS)[number]){return this.assigned().includes(x)}classLabel(g:readonly(typeof D3_ELEMENTS)[number][]):string{return`{${g.map(label).join(', ')}}`}sumLabel(){return this.visible().map(x=>x.length).join(' + ')||'0'}}
