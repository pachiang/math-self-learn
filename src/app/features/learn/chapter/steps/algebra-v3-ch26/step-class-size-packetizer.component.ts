import{Component,computed,signal}from'@angular/core';import{D3_ELEMENTS,type D3Element,conjugate,label}from'../algebra-v3-ch16/d3-model';import{centralizerOf,classOf}from'./class-model';
@Component({selector:'app-algebra-v3-class-size-packetizer',standalone:true,template:`
<article class="algebra-v3-lesson alg-ch26-lesson"><header class="hero"><p class="eyebrow">Abstract Algebra · 26.2</p><h2>Class size 是 frame cards 分包後的 packet 數</h2><p class="lede">讓 action map 變成 k↦khk⁻¹。image 是 conjugacy class；每條 fiber 是 centralizer coset。於是六張 frames 會排成「class names × frames per name」。</p></header><section class="prediction"><p class="kicker">先預測</p><h3>一個 h 能和越多 frames commute，它的 conjugacy class 會越大還是越小？</h3><div class="choice-row"><button type="button" (click)="prediction.set(false)">越小，更多 frames 重複同一 name</button><button type="button" (click)="prediction.set(true)">越大，commuting 會產生更多 names</button></div>@if(prediction()!==null){<p class="feedback" [class.warning]="prediction()">{{prediction()?'commuting frames 都把 h 留在同一 output，會擠進同一 fiber，而不是開新 packet。':'對。centralizer 越大，每包越厚；固定總數下 packet 數就越少。'}}</p>}</section>
<section class="lab"><div class="lab-heading"><div><p class="kicker">Class-size packetizer</p><h3>切換 h，看六張 frame cards 如何重排</h3></div><p>每條 output lane 是一個 conjugate name；lane 內 cards 是產生該 name 的 frames。</p></div><div class="h-picker">@for(x of elements;track x){<button type="button" [attr.aria-pressed]="h()===x" (click)="h.set(x)">h={{name(x)}}</button>}</div><div class="stage packetizer-stage"><section class="class-packets">@for(packet of packets();track packet.output){<article><header><span>CONJUGATE OUTPUT</span><strong>{{name(packet.output)}}</strong></header><div>@for(k of packet.frames;track k){<b>k={{name(k)}}</b>}</div><footer>{{packet.frames.length}} FRAMES IN FIBER</footer></article>}</section><section class="size-console"><p class="kicker">ORBIT–STABILIZER REUSED</p><div class="size-equation"><span>|G| = 6</span><i>=</i><span>|Cl({{name(h())}})| = {{classSize()}}</span><i>×</i><span>|C<sub>G</sub>({{name(h())}})| = {{centralizerSize()}}</span></div><div class="size-verdict">{{classSize()}} = 6 ÷ {{centralizerSize()}}</div></section></div></section>
<aside class="insight-card"><div class="insight-visual" aria-hidden="true"><span>|Cl(h)|</span><i>=</i><span>|G| / |C_G(h)|</span></div><p><strong>Class-size formula 就是 orbit–stabilizer 套在 conjugation action。</strong>class 是 orbit，centralizer 是 stabilizer；沒有新的 counting magic。</p></aside><section class="transfer"><p class="kicker">遷移</p><h3>若 h 在 center，class size 會是多少？</h3><div class="choice-row"><button type="button" (click)="transfer.set(1)">1</button><button type="button" (click)="transfer.set(6)">|G|</button></div>@if(transfer()){<p class="feedback" [class.warning]="transfer()!==1">{{transfer()===1?'對。所有 frames 都固定 h，所以只有一個 conjugate name。':'centralizer 是整個 G，公式給 |G|/|G|=1。'}}</p>}</section><section class="secondary"><p>SECONDARY LAYER</p><details><summary>Class-size formula</summary><div>對 conjugation action，Orb(h)=Cl(h)，Stab(h)=C_G(h)。有限群 orbit–stabilizer 立即給 |G|=|Cl(h)|·|C_G(h)|，所以 |Cl(h)|=[G:C_G(h)]。</div></details></section></article>`})
export class AlgebraV3ClassSizePacketizerComponent{readonly elements=D3_ELEMENTS;readonly h=signal<D3Element>(3);readonly prediction=signal<boolean|null>(null);readonly transfer=signal<number|null>(null);readonly outputs=computed(()=>classOf(this.h()));readonly packets=computed(()=>this.outputs().map(output=>({output,frames:this.elements.filter(k=>conjugate(k,this.h())===output)})));readonly classSize=computed(()=>this.outputs().length);readonly centralizerSize=computed(()=>centralizerOf(this.h()).length);name=label;}
