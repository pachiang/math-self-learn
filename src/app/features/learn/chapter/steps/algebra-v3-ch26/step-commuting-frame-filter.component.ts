import{Component,computed,signal}from'@angular/core';import{D3_ELEMENTS,type D3Element,conjugate,label}from'../algebra-v3-ch16/d3-model';import{centralizerOf}from'./class-model';
@Component({selector:'app-algebra-v3-commuting-frame-filter',standalone:true,template:`
<article class="algebra-v3-lesson alg-ch26-lesson"><header class="hero"><p class="eyebrow">Abstract Algebra · 26.1</p><h2>Centralizer 是讓 h 在 conjugation 下留在原位的 frames</h2><p class="lede">固定 action h，逐一換 frame k。若 khk⁻¹=h，這個 frame 看見相同 description；把所有通過的 k 收起來，就是 h 的 conjugation stabilizer。</p></header><section class="prediction"><p class="kicker">先預測</p><h3>khk⁻¹=h 與 kh=hk 是兩個不同條件嗎？</h3><div class="choice-row"><button type="button" (click)="prediction.set(false)">其實等價</button><button type="button" (click)="prediction.set(true)">完全無關</button></div>@if(prediction()!==null){<p class="feedback" [class.warning]="prediction()">{{prediction()?'把 khk⁻¹=h 右乘 k 就得到 kh=hk；兩種語言指向同一批 frames。':'對。centralizer 可讀成 commute filter，也可讀成 conjugation stabilizer。'}}</p>}</section>
<section class="lab"><div class="lab-heading"><div><p class="kicker">Commuting-frame filter</p><h3>選 h，再逐張測試 frame k</h3></div><p>✓ FIXED cards 進入 centralizer；→ CHANGED cards 被排除，文字與 border 同時編碼。</p></div><div class="h-picker">@for(x of elements;track x){<button type="button" [attr.aria-pressed]="h()===x" (click)="select(x)">h={{name(x)}}</button>}</div><div class="stage centralizer-stage"><section class="commute-grid">@for(row of rows();track row.k){<article [class.scanned]="row.scanned" [class.fixed]="row.scanned&&row.fixed"><strong>k={{name(row.k)}}</strong><span>{{row.scanned?name(h())+' → '+name(row.output):'?'}}</span><small>{{row.scanned?(row.fixed?'✓ FIXED / COMMUTES':'→ CHANGED'):'WAITING'}}</small></article>}</section><section class="centralizer-console"><p class="kicker">FRAME SCAN {{count()}} / 6</p><div><span>C<sub>G</sub>({{name(h())}})</span><b>{{resultLabel()}}</b><small>FRAMES THAT COMMUTE WITH h</small></div><button type="button" class="primary" [disabled]="count()>=6" (click)="advance()">測試下一個 frame</button><button type="button" (click)="count.set(6)">測試全部</button><button type="button" (click)="count.set(0)">重設</button></section></div></section>
<aside class="insight-card"><div class="insight-visual" aria-hidden="true"><span>centralizer of h</span><i>=</i><span>stabilizer of h under conjugation</span></div><p><strong>Centralizer 不是新機器，而是熟悉的 stabilizer 換了 action。</strong>現在的 points 是 group elements，moves 是 re-framings。</p></aside><section class="transfer"><p class="kicker">遷移</p><h3>identity e 的 centralizer 有多大？</h3><div class="choice-row"><button type="button" (click)="transfer.set(6)">整個 G</button><button type="button" (click)="transfer.set(1)">只有 e</button></div>@if(transfer()){<p class="feedback" [class.warning]="transfer()!==6">{{transfer()===6?'對。每個 element 都和 identity commute。':'e 在任何 frame 都不變，因此所有 k 都在 C_G(e)。'}}</p>}</section><section class="secondary"><p>SECONDARY LAYER</p><details><summary>Centralizer 的正式版本</summary><div>C_G(h) 由所有滿足 kh=hk 的 k∈G 組成；等價地 khk⁻¹=h。它是 h 在 conjugation action 下的 stabilizer，因此自動是 G 的 subgroup。</div></details></section></article>`})
export class AlgebraV3CommutingFrameFilterComponent{readonly elements=D3_ELEMENTS;readonly h=signal<D3Element>(3);readonly count=signal(0);readonly prediction=signal<boolean|null>(null);readonly transfer=signal<number|null>(null);readonly rows=computed(()=>this.elements.map((k,i)=>({k,output:conjugate(k,this.h()),fixed:conjugate(k,this.h())===this.h(),scanned:i<this.count()})));name=label;select(x:D3Element){this.h.set(x);this.count.set(0)}advance(){this.count.update(v=>Math.min(6,v+1))}resultLabel(){const a=this.rows().filter(x=>x.scanned&&x.fixed).map(x=>label(x.k));return`{${a.join(', ')}}`}fullSize(){return centralizerOf(this.h()).length}}
