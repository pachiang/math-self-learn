import{Component,computed,signal}from'@angular/core';import{rationalPartner}from'./rings-ch4-model';
@Component({selector:'app-rings-ch4-ambient-unit',standalone:true,template:`<article class="algebra-v3-lesson rings-lesson"><header class="hero"><p class="eyebrow">Rings & Ideals · 4.2</p><h2>Unit 是「在這個 ring 裡可逆」；ambient boundary 是答案的一部分</h2><p class="lede">固定 element 與 equation，只切換哪些 cards屬於目前 world。2 的 required partner永遠是 1/2；它在 ℚ 裡，卻不在 ℤ 裡。</p></header><section class="prediction"><div><p class="kicker">先預測</p><h3>同一個 2 在 ℤ 與 ℚ 中，unit verdict 必須相同嗎？</h3></div><div class="choice-row"><button type="button"(click)="prediction.set(false)">不必，world不同</button><button type="button"(click)="prediction.set(true)">必須相同</button></div>@if(prediction()!==null){<p class="feedback"[class.warning]="prediction()">{{prediction()?'Element外觀沒變，但inverse是否住在world會改變。':'對。這次只改membership，其餘全部固定。'}}</p>}</section><div class="control-row"><span class="kicker">AMBIENT</span><button type="button"[class.active]="world()==='Z'"(click)="world.set('Z')">ℤ</button><button type="button"[class.active]="world()==='Q'"(click)="world.set('Q')">ℚ</button><span class="kicker">ELEMENT</span>@for(v of elements;track v){<button type="button"[class.active]="element()===v"(click)="element.set(v)">{{v}}</button>}</div><section class="stage stage-grid"><div class="nested-world"role="img"[attr.aria-label]="ariaLabel()"><div class="q-world"><span class="world-label">ℚ · rationals</span><div class="z-world"><strong>ℤ · integers</strong><span class="unit-card">a={{element()}}</span>@if(partnerInZ()){<span class="partner-card">inverse {{partner()}}</span>}</div>@if(partner()&&!partnerInZ()){<span class="partner-card outside">inverse {{partner()}}</span>}@if(!partner()){<span class="partner-card outside none">NO PARTNER FOR 0</span>}</div></div><aside class="console"aria-live="polite"><p class="kicker">MEMBERSHIP CHECK</p><h3>{{isUnit()?'UNIT IN '+world():'NOT A UNIT IN '+world()}}</h3><p>Required partner：{{partner()??'不存在'}}。</p><p>{{reading()}}</p><div class="readout">{{element()}} {{isUnit()?'∈':'∉'}} {{world()}}ˣ</div></aside></section><section class="insight"><span class="insight-icon">∈</span><div><strong>Inverse 必須住在同一個 world</strong><span>Unit status 不是 element 脫離 ambient ring 後仍固定的標籤。</span></div></section><details><summary>World 擴大時會發生什麼？</summary><p>若R⊆S共用1，R中的unit在S中仍有同一inverse；但R中的nonunit可能在S中獲得原本住在boundary外的inverse。</p></details></article>`})export class RingsCh4AmbientUnitComponent{readonly world=signal<'Z'|'Q'>('Z');readonly element=signal(2);readonly elements=[2,-1,0]as const;readonly prediction=signal<boolean|null>(null);readonly partner=computed(()=>rationalPartner(this.element()));readonly partnerInZ=computed(()=>this.partner()!==null&&(this.element()===1||this.element()===-1));readonly isUnit=computed(()=>this.partner()!==null&&(this.world()==='Q'||this.partnerInZ()));readonly reading=computed(()=>!this.partner()?'0乘任何element都回不到1。':this.partnerInZ()?'Partner本來就在ℤ，因此兩個world都接得上dock。':this.world()==='Z'?'Equation沒變；1/2 card只是在目前boundary外。':'World擴大後，原本boundary外的partner現在可以接進dock。');readonly ariaLabel=computed(()=>`nested integers inside rationals，element ${this.element()} required partner ${this.partner()??'不存在'}，目前${this.isUnit()?'是':'不是'} ${this.world()}中的unit`);}
