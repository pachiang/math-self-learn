import { Component, computed, signal } from '@angular/core';
type Stage='kernel'|'isomorphism'|'image';
interface Model{ id:string;label:string;source:string;sourceSize:number;kernel:string;kernelSize:number;quotient:string;quotientSize:number;image:string;imageSize:number;target:string;targetSize:number;forgot:string;kept:string; }
@Component({selector:'app-algebra-v3-factorization-blueprint',standalone:true,template:`
<article class="algebra-v3-lesson alg-ch18-lesson"><header class="hero"><p class="eyebrow">Abstract Algebra · 18.3</p><h2>每個 homomorphism 都能拆成同一條壓縮藍圖：分桶、無損翻譯、嵌入</h2><p class="lede">First Isomorphism Theorem 不是一條神秘 shortcut。它把任何 structure-preserving map 的三種工作分開：哪些差異被忘掉、留下的結構是什麼、target 哪些區域真的可達。</p></header>
<section class="prediction"><p class="kicker">先判斷</p><h3>中央 isomorphism 是原 homomorphism 的「壓縮部分」嗎？</h3><div class="choice-row"><button type="button" (click)="prediction.set(false)">不是，壓縮已由 quotient 完成</button><button type="button" (click)="prediction.set(true)">是</button></div>@if(prediction()!==null){<p class="feedback" [class.warning]="prediction()">{{prediction()?'中央箭頭已無 collisions；真正的資訊遺失發生在 G→G/ker φ。':'對。中央只把 quotient 結構無損翻譯成 image。'}}</p>}</section>
<section class="lab"><div class="lab-heading"><div><p class="kicker">Factorization blueprint</p><h3>切換 map，再聚焦三個 stage 的分工</h3></div><p>每個 example 都沿同一 layout；改變的是 kernel 與 image，不是 theorem 的骨架。</p></div><div class="blueprint-controls"><div role="group" aria-label="選擇 homomorphism example">@for(item of models;track item.id){<button type="button" [attr.aria-pressed]="modelId()===item.id" (click)="modelId.set(item.id)">{{item.label}}</button>}</div><div role="group" aria-label="聚焦 factorization stage"><button type="button" [attr.aria-pressed]="stage()==='kernel'" (click)="stage.set('kernel')">1 · KERNEL QUOTIENT</button><button type="button" [attr.aria-pressed]="stage()==='isomorphism'" (click)="stage.set('isomorphism')">2 · ISOMORPHISM</button><button type="button" [attr.aria-pressed]="stage()==='image'" (click)="stage.set('image')">3 · IMAGE INCLUSION</button></div></div><div class="stage blueprint-stage"><section class="blueprint-flow"><article><span>SOURCE G</span><strong>{{model().source}}</strong><small>{{model().sourceSize}} elements</small></article><i [class.active]="stage()==='kernel'"><b>quotient by</b><strong>{{model().kernel}}</strong><small>{{model().forgot}}</small></i><article><span>G / ker φ</span><strong>{{model().quotient}}</strong><small>{{model().quotientSize}} buckets</small></article><i [class.active]="stage()==='isomorphism'"><b>φ̄ · ≅</b><strong>LOSSLESS</strong><small>bijective homomorphism</small></i><article><span>IMAGE</span><strong>{{model().image}}</strong><small>{{model().imageSize}} reached</small></article><i [class.active]="stage()==='image'"><b>include into</b><strong>↪</strong><small>no new outputs</small></i><article><span>CODOMAIN H</span><strong>{{model().target}}</strong><small>{{model().targetSize}} allowed</small></article></section><section class="blueprint-console" aria-live="polite"><p class="kicker">{{stageTitle()}}</p><div class="role-readout"><strong>{{stageRole()}}</strong><span>{{stageDetail()}}</span></div><div class="count-equation"><span>|G/ker φ|</span><strong>{{model().quotientSize}}</strong><i>=</i><span>|im φ|</span><strong>{{model().imageSize}}</strong></div><div class="map-verdict">✓ G / ker φ ≅ im φ</div><p>保留下來的 observable：{{model().kept}}</p></section></div></section>
<aside class="insight-card"><div class="insight-visual" aria-hidden="true"><span>kernel</span><i>explains collisions</i><span>quotient ≅ image</span><i>explains surviving structure</i></div><p><strong>First Isomorphism Theorem 是所有 group homomorphisms 的解剖圖。</strong>Kernel 說明丟了什麼；image 說明留下什麼；兩者之間沒有額外損失。</p></aside>
<section class="transfer"><p class="kicker">遷移</p><h3>若 homomorphism 是 surjective，theorem 會簡化成什麼？</h3><div class="choice-row"><button type="button" (click)="transfer.set(true)">G/ker φ ≅ H</button><button type="button" (click)="transfer.set(false)">G ≅ H</button></div>@if(transfer()!==null){<p class="feedback" [class.warning]="!transfer()">{{transfer()?'對。Surjective 時 image 就是整個 H。':'原 map 仍可能 many-to-one；必須先 quotient kernel。'}}</p>}</section>
<section class="secondary"><p>SECONDARY LAYER</p><details><summary>First Isomorphism Theorem</summary><div>對 group homomorphism φ:G→H，kernel K◁G，且 map gK↦φ(g) 定義出 natural isomorphism G/K≅im φ。</div></details><details><summary>Proof obligations 對應哪個 stage？</summary><div>Kernel normal 讓 quotient 存在；同 coset 同 image 讓 φ̄ well-defined；kernel 精確描述 collisions 給 injective；image 的定義給 surjective；homomorphism law 給 structure preservation。</div></details></section></article>`})
export class AlgebraV3FactorizationBlueprintComponent{readonly prediction=signal<boolean|null>(null);readonly transfer=signal<boolean|null>(null);readonly modelId=signal('z12');readonly stage=signal<Stage>('kernel');readonly models:Model[]=[{id:'z12',label:'ℤ₁₂→ℤ₈ · n↦2n',source:'ℤ₁₂',sourceSize:12,kernel:'{0,4,8}',kernelSize:3,quotient:'ℤ₁₂/{0,4,8}',quotientSize:4,image:'{0,2,4,6}',imageSize:4,target:'ℤ₈',targetSize:8,forgot:'difference mod 4',kept:'even output class'},{id:'d3',label:'D₃→C₂ · sign',source:'D₃',sourceSize:6,kernel:'R',kernelSize:3,quotient:'D₃/R',quotientSize:2,image:'C₂',imageSize:2,target:'C₂',targetSize:2,forgot:'exact pose / axis',kept:'reflection parity'},{id:'zero',label:'ℤ₄→ℤ₆ · zero map',source:'ℤ₄',sourceSize:4,kernel:'ℤ₄',kernelSize:4,quotient:'ℤ₄/ℤ₄',quotientSize:1,image:'{0}',imageSize:1,target:'ℤ₆',targetSize:6,forgot:'every source difference',kept:'only identity'}];readonly model=computed(()=>this.models.find(m=>m.id===this.modelId())??this.models[0]);stageTitle():string{return this.stage()==='kernel'?'STAGE 1 · EXPLAIN ALL COLLISIONS':this.stage()==='isomorphism'?'STAGE 2 · LOSSLESS STRUCTURE TRANSLATION':'STAGE 3 · DISTINGUISH IMAGE FROM CODOMAIN';}stageRole():string{return this.stage()==='kernel'?'把 φ 無法分辨的 inputs 精確收進同一桶':this.stage()==='isomorphism'?'每個 quotient bucket 唯一對齊一個 image element':'把實際可達結構放回原 target';}stageDetail():string{return this.stage()==='kernel'?`每桶 ${this.model().kernelSize} 個 source elements；資訊遺失只發生於此。`:this.stage()==='isomorphism'?'沒有 collisions、沒有漏點、operation preserved。':`${this.model().imageSize}/${this.model().targetSize} 個 codomain elements 實際可達。`;}}
